/**
 * The current plan year used internally for database organization.
 * NOT displayed to users - all user-facing text uses rolling 12-month language.
 * Update this value annually when preparing the next edition.
 */
export const CURRENT_PLAN_YEAR = 2026;
